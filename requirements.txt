streamlit>=1.37
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9
sqlmodel>=0.0.19
alembic>=1.13
psycopg2-binary>=2.9
duckdb>=0.10
duckdb-engine>=0.13
pydantic>=2.7
anthropic>=0.28
openai>=1.50
python-dotenv>=1.0
# Security pin — msgpack <1.2.1 has GHSA-6v7p-g79w-8964 (transitive dep). Plan 00041.
msgpack>=1.2.1
pytest>=8.0
pytest-cov>=5.0
black>=24.0
# Pinned exactly: isort dialects differ across majors, and version drift between
# the venv, the pre-commit hook, and CI red-lined main (Plan 42 retro). Keep this,
# .pre-commit-config.yaml's rev, and CI in lockstep when bumping.
isort==8.0.1
flake8>=7.0
interrogate>=1.7
pip-audit>=2.7
